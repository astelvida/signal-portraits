// Test stub for the `server-only` package. The real package throws when
// imported outside a React Server Component build; under Vitest we just
// want the import to be a no-op so server modules can be unit-tested.
export {};
